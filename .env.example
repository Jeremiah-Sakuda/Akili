# Copy to .env and set your key. Required for Docker and for local API (Gemini).
GOOGLE_API_KEY=your_key_here

# Optional: override SQLite path (default: akili.db in current directory)
# AKILI_DB_PATH=/data/akili.db

# Optional: set to 1 to include full error messages in API responses (e.g. ingest 500)
# AKILI_DEBUG=1

# Optional: require Firebase ID token for API (set 1 + FIREBASE_PROJECT_ID; needs GOOGLE_APPLICATION_CREDENTIALS or ADC).
# AKILI_REQUIRE_AUTH=0

# Optional: CORS origins (comma-separated). Default: localhost:3000,3001. For production add your frontend origin.
# AKILI_CORS_ORIGINS=https://your-project.web.app

# Optional: max upload size for PDF ingest in bytes (default 100MB).
# AKILI_MAX_UPLOAD_BYTES=104857600

# Optional: Gemini model for extraction (default: gemini-3.0-flash).
# Gemini 3 excels at document vision, structured output, and coordinate-grounded extraction for technical PDFs.
# AKILI_GEMINI_MODEL=gemini-3.0-flash
# AKILI_GEMINI_MODEL=gemini-3-flash-preview
# AKILI_GEMINI_MODEL=gemini-3-pro-preview

# Optional: Gemini 429 handling. Ingest does one API call per PDF page and retries on rate limit.
# AKILI_GEMINI_MAX_RETRIES=4          # retries per page (default 4)
# AKILI_GEMINI_BACKOFF_BASE=4.0        # base seconds for exponential backoff (default 4)
# AKILI_GEMINI_PAGE_DELAY_SECONDS=2.0  # seconds to wait between pages to avoid bursting (default 2)

# Optional: Shadow Formatting (query-time natural-language phrasing of verified answers).
# Timeout in seconds for the Gemini format call; on timeout/failure the UI shows the raw answer.
# AKILI_FORMAT_TIMEOUT_SEC=2.5

# Firebase (for frontend login + analytics). Get from Firebase Console → Project settings → General.
# Vite loads from repo root when you run npm run dev from frontend/ (see vite.config envDir).
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_MEASUREMENT_ID=